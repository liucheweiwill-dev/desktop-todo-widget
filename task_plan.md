# task_plan.md — 階段計畫

## 原則

**先驗證最可能推翻整個設計的假設。** 本專案該假設是「嵌入桌面」——
它沒有受支援的公開 API（findings.md D2）。因此 S1 spike 排在所有功能之前，
且**不含任何 todo 功能**。

## 階段

| ID | 內容 | 依賴 | 狀態 |
|---|---|---|---|
| **S1** | **Spike：桌面附著** | — | **完成**（+S1b Z-order 修正）。結論：`workerw` 不可交付並移除，`bottommost` 為唯一模式 |
| **D1a** | 路徑解析、原子 JSON 儲存（含首次建立路徑）、測試專案 scaffolding | — | **完成**（11 tests + mutation probe） |
| **D1b** | Todo 模型、schema 版本策略、單一實例 | D1a | **完成**（14 tests + mutation probe） |
| W1 | 視窗外殼、拖曳移動、位置持久化（+W1b 消除拖曳閃爍） | D1b + S1 | **完成**（人工 M1–M7 全過） |
| U1 | Todo UI：新增／刪除／勾選／內嵌編輯（+U1b 深色主題、U1c 選取可讀與輸入框移底） | W1 | **完成**（人工 U-1～U-8 全過） |
| R1 | 拖曳排序：插入指示線、邊緣自動捲動、順序持久化 | U1 | **完成**（人工 D1–D9 全過） |
| U2 | 每項字型大小與顏色（+U2b 色票重調） | R1 | **完成**（部分人工驗收未回報） |
| **P1** | 打包：self-contained 發佈、體積量測、**形態由使用者拍板** | U2 | **未開始 ← 目前在這裡** |

**D1 與 W1 的順序在 review 後對調**（Codex 第 3 項）：W1 需要位置／大小持久化與
單一實例，但那些的路徑解析、原子寫入與 mutex 都由 D1 定義。原順序會逼實作者先寫
一份暫時性的儲存實作，而且 W1 無法獨立驗證。

**D1 拆成 D1a／D1b**：單張卡的檔案數會超過上限，且原子寫入值得獨立測試。

**S1 與 D1a／D1b 沒有依賴關係**（一個是 Win32 附著、一個是純檔案邏輯），
因此 S1 卡在人工驗證時，資料層可以繼續推進。W1 之後才需要 S1 的結論。

其餘依賴為線性。

## S1 的特殊地位

S1 **不是功能開發，是決策工具**。它的產出不是程式碼，是一個答案：

> `workerw` 模式在這台端機上夠不夠穩？

- **夠穩** → findings.md D2 可考慮把預設改為 `workerw`
- **不夠穩** → 產品定義立刻改為「bottommost tool window」，`workerw` 降級為實驗選項

**S1 失敗不代表專案失敗**，代表產品定義要調整。這是它排第一的理由。

**不論結果如何都不要因此改用 Rust／Tauri／ImGui**——桌面附著是 shell 問題，
換語言逃不掉（findings.md D1）。

## 驗證指令

```
dotnet build
```

```
dotnet test
```

```
dotnet publish -c Release -r win-x64 --self-contained true
```

UI 與視窗行為不寫自動化測試（findings.md D6），改用各階段任務卡裡的人工 exit gate 清單。

## 阻擋項

| 項目 | 需要誰 | 說明 |
|---|---|---|
| 端機歸屬 | 使用者 | 端機是使用者本人的另一台電腦，還是他人的？影響 D2 的預設模式與 R3（SmartScreen）的處理 |
| 打包形態 | 使用者 | 單一 exe（接受 `%TEMP%\.net` 解壓）或免安裝資料夾。P1 前需拍板，見 findings.md D5 |
| 專案名稱 / 顯示名稱 | 使用者 | 影響 `%LOCALAPPDATA%` 資料夾名與視窗標題，W1 前需確定 |

## 進度

第一個階段完成時建立 `progress.md`。
